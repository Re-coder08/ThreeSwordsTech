name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - develop

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate HTML
        run: |
          if ! command -v html5validator &> /dev/null; then
            pip install html5validator
          fi
          html5validator --root . index.html 2>&1 | grep -v "error: CSS:" || true

  deploy-develop:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/develop'
    needs: validate
    steps:
      - uses: actions/checkout@v4
      - name: Update CNAME for subdomain
        run: echo "dev.threeswordstech.com" > CNAME
      - name: Deploy develop to dev.threeswordstech.com
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./
          publish_branch: gh-pages-dev
          cname: dev.threeswordstech.com
